"""
Единый slowapi-limiter приложения.

Раньше инстансов было два — в `main.py` (он же `app.state.limiter`) и в
`api/auth.py` (им декорировались эндпоинты). У каждого своё хранилище, так
что лимиты считались в двух местах и не сходились. Здесь один общий.

Ограничение, о котором стоит помнить: хранилище slowapi — в памяти процесса.
Во время blue-green деплоя оба цвета живут одновременно, и лимит на это
время фактически удваивается. Для защиты от абьюза это приемлемо; там, где
важен точный потолок (деньги), считаем в Redis — см. services/quota.py.
"""
from slowapi import Limiter

from app.utils.request_ip import get_client_ip

limiter = Limiter(key_func=get_client_ip)
