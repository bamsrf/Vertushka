# План: Система ачивок Вертушки v2

> Переработка [PLAN_ACHIEVEMENTS.md](PLAN_ACHIEVEMENTS.md) (1115 строк → ~570). Согласовано в обсуждении 2026-05-10/11.
>
> **Что изменилось относительно v1:**
>
> - Структура: **серии** (видимые с инструкцией) + **рандомные** (полностью скрытые, влётные).
> - Названия: раздел = «Ачивки» (не «Зал трофеев»), группы = «Серии» (не «Созвездия»).
> - Сезонные окна выкинуты: бронь подарков идёт заранее, ломается привязка к датам.
> - Очки/уровни выкинуты: только косметика напрямую от конкретных ачивок.
> - Welcome ceremony выкинут: главный момент — A1, без принудительных серий.
> - Стилистика — **эмалевые пины**, тиры названы по-русски и кодируются цветом из единого градиента (светло-голубой → розовый → тёмный).
> - Ачивки видны не только на публичном web-профиле, а и на внутри-приложенческих профилях (`Mobile/app/user/`).
> - Шеринг ачивки = отдельный экран генерации картинки под Instagram Stories (9:16) с автокропом под 1:1 и 4:5.
> - Импорт Discogs / редактирование метаданных / spinner на детальной — отложено до появления фич.
> - Стартовый каталог: **10 серий + ~12 рандомных = ~65 ачивок** (вместо 190).

---

## 0. Статус и маркеры арта

> Актуально на 2026-08-07. В коде **130 определений**, из них 121 идёт в зачёт
> уровня. Порог ступеней пересчитывается при каждом крупном пополнении каталога —
> см. [ACHIEVEMENT_XP_RARITY.md](ACHIEVEMENT_XP_RARITY.md).

**Маркеры в таблицах ниже:**

| Маркер | Значение |
| --- | --- |
| 🖼️ | Нужен отрисованный пин — PNG-дизайна или SVG нет, ачивка рендерится заглушкой |
| 📍 | У серии нет заглушки в `SERIES_PLACEHOLDERS` — закрытые ачивки останутся вовсе без картинки |

Сейчас арт нужен **94 ачивкам** и **7 сериям**. Инвентарь: 33 PNG-дизайна и
10 SVG-пинов в `Mobile/assets/achievements/`.

**Не реализовано и отложено:**

| Код | Причина |
| --- | --- |
| `R_birth_year` | нет поля даты рождения у `User` — нужна миграция и место в UI, где её спросить. Отложено |
| `R_first_hundred`, `R_first_thousand` | ждут публичного запуска (см. §5.4) |
| `E_*` (9 шт), `MX_window_shopper` | нужны счётчики жестов в Mobile; эндпоинт `POST /achievements/events` для них уже готов |

---

## 1. Принципы


| Принцип                           | Смысл                                                                                            |
| --------------------------------- | ------------------------------------------------------------------------------------------------ |
| **Глубина > количество**          | «100 пластинок» — слабо. «10 разных стран пресса» — сильно.                                      |
| **Видимое и невидимое**           | Серии — открытая дорога с инструкцией. Рандомные — скрыты целиком, без счётчиков, без подсказок. |
| **Никаких daily-механик**         | Нет streak'ов, нет «открой 7 дней подряд», нет дедлайнов.                                        |
| **Награды — косметика**           | Бейджи-пины, рамки, темы, титулы, особые иконки коллекций. Никакой валюты, премиума, скидок.     |
| **Анти-grind**                    | Все массовые ачивки требуют разнообразия (N **разных** стран/жанров), не N штук.                 |
| **Один раз заработал — навсегда** | Удалил пластинки и упал ниже порога — ачивка остаётся. L-ачивки фиксируют исторический максимум. |
| **Без процентов**                 | В UI только «открыто X из Y». Никаких процентов и графиков статуса.                              |


---

## 2. Архитектура

### 2.1. Серии (achievement series)

- Видимы в каталоге с названием, описанием и инструкцией («собери N стран», «подари N разных людей»).
- 4–8 ачивок внутри, выстроенных в прогрессию (B1 → B2 → … либо параллельные ветки).
- Закрытие серии (все ачивки внутри) = **мета-ачивка** уровня Эпическая/Легенда + уникальная косметическая награда.
- Прогресс по конкретной ачивке внутри серии виден только когда юзер получил хотя бы одну из этой серии (= вступил в дорогу).

### 2.2. Рандомные

- В каталоге одна общая карточка: **«❓ Сюрпризы»** с подзаголовком «Их обычно находят сами».
- Полностью скрыты до анлока: ни названия, ни условия, ни счётчика.
- Анлок — лёгкий всплывающий тост в приложении, без push-сирены. Сохраняем эффект «не может быть».

### 2.3. Когда что использовать


| Тип ачивки                      | Рандомная                 | Серия               |
| ------------------------------- | ------------------------- | ------------------- |
| Прогрессия по тиру / количеству | —                         | ✅ B, J              |
| Тематическая дорога             | —                         | ✅ D-страны, F-жанры |
| Шутка / синхрония               | ✅ «Тёзка», «Тридцать три» | —                   |
| Зависит от поведения мира       | ✅ EE_time_machine         | —                   |
| Тестирует все режимы фичи       | —                         | ✅ INV-приглашения   |


### 2.4. Где живут ачивки

Три точки показа:


| Место                                                             | Что показываем                                                                              | Кем доступно                                           |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| **Свой профиль** (`Mobile/app/profile.tsx`)                       | Главный блок «🏆 Ачивки» с превью топ-3 свежих и CTA на полный грид.                        | Только владелец.                                       |
| **Чужой профиль в приложении** (`Mobile/app/user/[username].tsx`) | Блок «🏆 Ачивки» с топ-3 свежих + общий счётчик «12 ачивок». Тап → полный грид этого юзера. | Любой залогиненный юзер при заходе через поиск/follow. |
| **Публичный web-профиль** (`ProfileShare`)                        | Компактный блок топ-3 свежих. Без полного грида (страница и так длинная).                   | Любой по ссылке, без логина.                           |


**Видимость рандомных:** на любом профиле они показываются как «❓ Получено 7 сюрпризов», без раскрытия названий. Intrigue-эффект для зрителя.

**Видимость L-категории (стоимость коллекции):** только если `ProfileShare.show_collection_value=true`, иначе скрыто и на in-app, и на web.

---

## 3. Тиры, стилистика, бенчмарки

### 3.1. Тиры

5 тиров, кодируются цветом из единого градиента приложения (светло-голубой → розовый → тёмный). Чем темнее, тем эпичнее и реже.


| Тир              | Цвет                     | Доля юзеров (цель) |
| ---------------- | ------------------------ | ------------------ |
| 💧 **Простая**   | Светло-голубой `#A5C8E1` | ≥30%               |
| 🔵 **Заметная**  | Голубой `#5B7DD8`        | 5–30%              |
| 🌸 **Редкая**    | Розовый `#E89AC0`        | 1–5%               |
| 🌌 **Эпическая** | Тёмно-синий `#1B237D`    | 0.1–1%             |
| ⚫ **Легенда**    | Графит `#0A0A1A`         | <0.1%              |


После Phase 1 — прогон бэкфилла на реальной базе, калибровка порогов так, чтобы доли совпали. Меняем цифру, не описание.

> **Альтернативный набор имён** (если хочется винил-метафоры): Сингл → EP → LP → Box-сет → Тест-прессинг. Финальное решение — за тобой.

### 3.2. Дизайн пинов

Каждая ачивка — **эмалевый пин со своим сюжетом**: винил/кассета/диск/проигрыватель/гитара/окно/корабль/маяк и т. д. — пин обыгрывает название через аналоговый предмет. Референсы пользователя: UFO welcome, Tyler the Creator IGOR/Flower Boy, 163 spaceship, Mokita pins set.

**Правила художника:**

- **Форма ≠ круг.** Силуэт каждого пина свой и заходит за рамки: окно, корабль, сердце, звезда, треугольник-арка проигрывателя, шеврон, ромб. Прямоугольная карточка-frame в UI просто содержит SVG, у которого края могут «торчать».
- **Сюжет в SVG**, не повторяющийся между ачивками. Опираемся на аналоговые форматы:
  - `A1 Поехали` — игла опускается на пластинку.
  - `A2 Хотелка` — сердце с виниловой канавкой внутри.
  - `A3 Аватар` — портретная рамка/силуэт головы.
  - `A4 Распахнул` — открытое окно, из которого вылетают пластинки как листы бумаги (или вид на город из аналоговых форматов).
  - `A5 Полка-двойник` — две стопки пластинок рядом.
  - `META_foundation На борту` — корабль из винила, парус-обложка.
  - `B1–B6` — растущая стопка/башня пластинок (10 → 1000).
  - `J1 Забронировал` — подарочная коробка, перевязанная виниловой канавкой.
  - `K2 Услышали` — ухо в виде воронки граммофона.
  - `K4 Голос сцены` — микрофон.
  - `R_self_titled Тёзка` — две одинаковые пластинки/зеркало.
  - `R_thirty_three Тридцать три` — 33⅓ в круге винила.
  - `R_pi` — π в форме пластинки.
- **Цветной overlay по тиру.** Сама форма у пина «своя» (не зависит от тира). Тир выражается через **цветную ауру/градиент позади пина**:
  - 💧 Простая → светло-голубой halo
  - 🔵 Заметная → насыщенный голубой
  - 🌸 Редкая → розовый
  - 🌌 Эпическая → тёмно-синий с лёгким shimmer
  - ⚫ Легенда → графит + анимированный holographic shimmer
- **Никакого одного повторяющегося элемента** на всех пинах. Канавки/винил появляются естественно (где это часть сюжета), но не как «фирменный обвес».
- Размеры для UI: 64×64 в гриде, 256×256 в анлок-анимации, 700×700 в share-card.
- Формат: SVG-компонент в `Mobile/components/achievement-scenes/<code>.tsx` (рендерится через react-native-svg, тир получает через prop). Это позволяет двигать/анимировать элементы (например, спираль крутится при анлоке).
- Серверная share-card до прихода SVG рисует плейсхолдер (см. `Backend/app/services/achievements/share_card.py`).

### 3.3. Бенчмарки (как сделано у других)


| Приложение                   | Что взять                                                                                              |
| ---------------------------- | ------------------------------------------------------------------------------------------------------ |
| **Apple Fitness — Awards**   | 3D-визуализация значков, тап → детальная карточка с анимацией. Главный визуальный focal point профиля. |
| **Strava — Achievements**    | Сразу готовая share-card на каждый анлок.                                                              |
| **Pokémon GO — Medals**      | Тиры через цвет рамки самой медали (bronze/silver/gold/platinum) — иерархия видна без подписи.         |
| **Untappd — Badges**         | Грид квадратных значков с category-фильтрами.                                                          |
| **Discord — Profile Badges** | Тонкая полоска значков рядом с ником — компактное превью.                                              |
| **Robinhood — Trophy Room**  | Отдельный экран с подсветкой свежих наград, не запихивают в общий профиль.                             |
| **GitHub — Achievements**    | Маленькие пины рядом с аватаром в превью контрибьютора.                                                |


**Что берём для Вертушки:**

- Apple Fitness — focal point своего профиля.
- Strava — каждый анлок генерит share-card.
- Pokémon GO — тиры через цвет (см. §3.1).
- Untappd — грид с фильтрами по сериям.
- Discord — превью топ-3 на чужом профиле.

---

## 4. Каталог серий (10 шт)

### 4.1. 🌱 Серия «Первые шаги»

Знакомство с базовыми возможностями. Закрытие = вход в раздел «Ачивки».


| Код                 | Название      | Тир | Условие                                                          |
| ------------------- | ------------- | --- | ---------------------------------------------------------------- |
| `A1_first_record`   | **Поехали**   | 💧  | Добавил первую пластинку в коллекцию.                            |
| `A2_first_wishlist` | **Хотелка**   | 💧  | Добавил первую запись в вишлист.                                 |
| `A3_avatar_set`     | **Аватар**    | 💧  | Поставил аватар.                                                 |
| `A4_public_profile` | **Распахнул** | 💧  | Активировал публичный профиль.                                   |
| `META_foundation`   | **На борту**  | 🔵  | Все 4 ачивки серии. Награда: бронзовая рамка карточек коллекции. |


### 4.2. 📚 Серия «Размер коллекции»

Главная вертикаль. Условие — каждая запись уникальна по `record_id`.


| Код            | Название       | Тир | Порог                                                                  |
| -------------- | -------------- | --- | ---------------------------------------------------------------------- |
| `B1_starter`   | **Десятка**    | 💧  | 10 уникальных пластинок                                                |
| `B2_collector` | **Полтинник**  | 💧  | 50                                                                     |
| `B3_archivist` | **Архивариус** | 🔵  | 100                                                                    |
| `B4_curator`   | **Куратор**    | 🌸  | 250                                                                    |
| `B5_keeper`    | **Хранитель**  | 🌌  | 500                                                                    |
| `B6_warden` 🖼️    | **Смотритель** | ⚫   | 1 000                                                                  |
| `META_scale`   | **Фонотека**   | ⚫   | Все 6. Награда: платиновая рамка + анимированная пластинка на профиле. |


**Анти-фарм:** B2+ считает `COUNT(DISTINCT record_id)` за записи `created_at` старше 24 часов. «Накачал и удалил» не зачитывается.

### 4.3. 💎 Серия «Охота за редкостями»

> Зависит от [RARITY_BADGES_PLAN.md](RARITY_BADGES_PLAN.md) — три флага: `is_collectible` (💚 коллекционка), `is_limited` (🟣 лимитка), `is_hot` (🟠 популярно). Первопресс выпилен — Discogs API не даёт надёжной разметки.


| Код                    | Название            | Тир | Условие                                                                         |
| ---------------------- | ------------------- | --- | ------------------------------------------------------------------------------- |
| `C1_limited_x5` 🖼️        | **Тираж ограничен** | 💧  | 5 лимиток.                                                                      |
| `C2_limited_x25`       | **По счёту**        | 🌸  | 25 лимиток.                                                                     |
| `C3_collectible_x1`    | **Сокровище**       | 🔵  | 1 коллекционка.                                                                 |
| `C4_collectible_x5` 🖼️    | **Энтомолог**  | 🌸  | 5 коллекционок.                                                                 |
| `C5_collectible_x15`   | **Кладовая**        | 🌌  | 15 коллекционок.                                                                |
| `C6_hot_in_wishlist`   | **Хочу горячего**   | 🔵  | В вишлисте одновременно 5 пластинок `is_hot=true`.                              |
| `C7_hot_in_collection` 🖼️ | **Тренд на полке**  | 🌸  | 10 пластинок `is_hot=true` в коллекции (флаг учитывается на момент добавления). |
| `META_rarity` 🖼️          | **Грааль**          | 🌌  | C2 + C5 + C7. Награда: голографическая рамка карточек.                          |


### 4.4. 🌍 Серия «Кругосветка»

Использует `Record.country`.


| Код                    | Название              | Тир | Условие                                                   |
| ---------------------- | --------------------- | --- | --------------------------------------------------------- |
| `D1_country_x5` 🖼️        | **Космополит**        | 💧  | Пластинки из 5 разных стран.                              |
| `D2_country_x15` 🖼️       | **Глобус**            | 🔵  | Из 15 стран.                                              |
| `D3_country_x30`       | **Кругосветка**       | 🌸  | Из 30 стран.                                              |
| `D4_japanese_x10`      | **Из Японии**          | 🌸  | 10 японских прессов.                                      |
| `D5_melodiya_x10`      | **Мелодия**           | 🌸  | 10 пластинок лейбла Melodiya / `country=USSR`.            |
| `D6_uk_collectible_x3` | **Британский почерк** | 🌸  | 3 коллекционки `country=UK`.                              |
| `D7_german_x10` 🖼️        | **Made in Germany**   | 🌸  | 10 пластинок из Germany / West Germany.                   |
| `META_geography` 🖼️       | **Атлас**             | 🌌  | D3 + любые 3 из D4–D7. Награда: тема приложения «Globus». |


### 4.5. 📅 Серия «Машина времени»

Использует `Record.year`.


| Код              | Название           | Тир | Условие                                                                           |
| ---------------- | ------------------ | --- | --------------------------------------------------------------------------------- |
| `E1_60s`         | **Шестидесятники** | 💧  | 5 пластинок 1960–1969.                                                            |
| `E2_70s`         | **Золотой век**    | 💧  | 10 пластинок 1970–1979.                                                           |
| `E3_80s`         | **Неон**           | 💧  | 10 пластинок 1980–1989.                                                           |
| `E4_modern`      | **Сегодняшний**    | 💧  | 5 пластинок последних 3 лет.                                                      |
| `E5_pre_1960`    | **Доисторический** | 🌸  | 1 пластинка ранее 1960.                                                           |
| `E6_decade_full` 🖼️ | **Десятилетие**    | 🌌  | По одной пластинке из каждого года любого 10-летия.                               |
| `META_eras` 🖼️      | **Век винила**     | ⚫   | По 1 пластинке из каждого десятилетия с 1950-х по 2020-е. Награда: тема «Винтаж». |


### 4.6. 🎼 Серия «Жанры»

Использует `Record.genre` и `Record.style`.


| Код                 | Название       | Тир | Условие                                                                     |
| ------------------- | -------------- | --- | --------------------------------------------------------------------------- |
| `F1_diversity_5` 🖼️    | **Меломаньяк** | 💧  | 5 разных жанров.                                                            |
| `F2_diversity_10`   | **Всеядный**   | 🔵  | 10 разных жанров.                                                           |
| `F3_jazz_x25`       | **Эй, Арнольд**   | 🌸  | 25 пластинок Jazz.                                                          |
| `F4_electronic_x25` | **Машинист**   | 🌸  | 25 пластинок Electronic.                                                    |
| `F5_classical_x15` 🖼️  | **Классик**    | 🌸  | 15 пластинок Classical.                                                     |
| `F6_rock_x25` 🖼️       | **Громко**     | 🌸  | 25 пластинок Rock.                                                          |
| `META_genres` 🖼️       | **Эрудит**     | 🌌  | F2 + любые 3 из F3–F6. Награда: title «Эрудит» с возможностью кастомизации. |


### 4.7. 🎁 Серия «Дарящая рука» ✅ Реализовано (Phase 2)

Использует `GiftBooking`. Анти-фарм: **разные получатели** обязательны
(считается по `recipient_user_id`, денормализован на `GiftBooking` — после
COMPLETED связь с владельцем вишлиста теряется, поле нужно фиксировать
заранее).

`J6_perfect_match` («В точку», `priority=high`) **удалена**: `priority` в
данных — int 0..10, у UI-понятия «High» никогда не было реального значения,
ачивка ссылалась на несуществующую сущность. На замену добавлены J7/J8/J9.


| Код                   | Название           | Тир | Условие                                                                           |
| --------------------- | ------------------ | --- | ---------------------------------------------------------------------------------- |
| `J1_first_gift`       | **Забронировал**   | 💧  | Забронировал первый подарок.                                                       |
| `J2_gift_done`        | **Долетело**       | 💧  | Первый completed подарок.                                                          |
| `J3_three_recipients` | **Дарящая рука**   | 🔵  | Завершил подарки 3 разным получателям.                                             |
| `J4_ten_recipients` 🖼️   | **Праздник**       | 🌸  | 10 разных получателей.                                                             |
| `J5_first_received` 🖼️   | **С теплом**       | 💧  | Получил первый подарок.                                                            |
| `J7_boomerang` 🖼️        | **Бумеранг**       | 🌸  | Подарил тому, кто раньше дарил тебе.                                               |
| `J8_loved` 🖼️            | **Любимчик**       | 🔵  | Получил подарки от 3 разных дарителей.                                             |
| `J9_santa` 🖼️            | **Дед Мороз**      | 🔵  | Подарок дошёл до адресата в окно 25.12–14.01 (Новый год + старый НГ).              |
| `META_gifts` 🖼️          | **Щедрость**       | 🌌  | Ядро серии: J2 + J3 + J4 + J5. Награда: анимированная иконка «Box» для коллекций.  |

`J1_first_gift` переименована из «Подарил» в «Забронировал»: ачивка
анлокается на `gift_booked` (момент брони), а не на реальное дарение —
старое название создавало ложное впечатление, что подарок уже дошёл.


### 4.8. 👥 Серия «Сообщество»

> Подзаголовок в UI: **«Подписки и общение.»** Чистая социалка — подписки и переписка. Вклад в каталог и спрос вынесены в отдельный раздел [§4.8b «Вклад»](#48b--раздел-вклад). Анти-фарм фолловеров: фолловер должен иметь ≥10 пластинок и аккаунт ≥30 дней.

**Трек 1 — Подписки/подписчики (сцена).** Путь от зрителя в зале до хедлайнера с залом своих фанатов. `Follow`.


| Код                 | Название       | Тир | Условие                      | Образ пина                     |
| ------------------- | -------------- | --- | ---------------------------- | ------------------------------ |
| `K1_following_x5`   | **Зритель**    | 💧  | Подписан на 5 пользователей. | купил билет, стоишь в толпе    |
| `K2_first_follower` | **Первый ряд** | 💧  | Получил первого подписчика.  | один фанат пришёл на тебя      |
| `K3_followers_x5`   | **Квартирник** | 💧  | 5 подписчиков.               | играешь в комнате, кучка людей |
| `K4_followers_x50`  | **Хедлайнер**  | 🌸  | 50 подписчиков.              | твоё имя на афише, полный зал  |
| `K7_mutual_x10`     | **Бэкстейдж**  | 🔵  | 10 взаимных подписок.        | пустили за кулисы, вы свои     |


**Трек 2 — Сообщения.** Кол-во отправленных сообщений в чатах.


| Код             | Название         | Тир | Условие        | Образ пина                    |
| --------------- | ---------------- | --- | -------------- | ----------------------------- |
| `K11_msgs_x10`  | **Есть контакт** | 💧  | 10 сообщений.  | связь установлена             |
| `K12_msgs_x50`  | **Продажник**    | 🔵  | 50 сообщений.  | на телефоне, как с Уолл-стрит |
| `K13_msgs_x200` | **Уолл-стрит**   | 🌸  | 200 сообщений. | торговый зал, гул сделок      |


| Код              | Название     | Тир | Условие                                                |
| ---------------- | ------------ | --- | ------------------------------------------------------ |
| `META_community` | **Резидент** | 🌌  | K4 + K7 + K13. Награда: бейдж «Resident» рядом с ником. |


> **Скрыты с v2.1:** `K5_views_x100` «Витрина» и `K6_views_x1000` «На главной» (просмотры публичного профиля). Подсчёт `ProfileShare.view_count` ненадёжен — нет дедупа по сессии, инкремент идёт от ботов/краулеров и от self-view через `/profile/public/{username}`. Не удалены из БД, но `is_hidden=True` и убраны из каталога. META теперь зависит только от ачивок внутри своей секции (K4 + K7 + K13).
>
> **Выпилены с v2.1:** трек «Космос» (`K17_pioneer`–`K20_pioneer`, «первым добавил релиз на платформу»). На ранней базе метрика тривиально true — единственный/первый юзер «первый» во всём. Вернуть можно при росте базы, переопределив как «первым добавил релиз, который потом добавили ещё ≥N человек» (тренд-сеттер, а не одиночка).

### 4.8b. 🏗 Раздел «Вклад»

> Подзаголовок в UI: **«Ценность для коммьюнити.»** Серия `contribution`. Вынесена из «Сообщества»: это не «кто на кого подписан», а вес юзера для каталога — что он добавил и насколько его коллекцию хотят другие.

**Трек 1 — Вклад в каталог (ручные релизы).** `Record.source='user'` + `moderation_status='approved'`. Анти-фарм встроен модерацией.


| Код               | Название      | Тир | Условие                    | Образ пина                     |
| ----------------- | ------------- | --- | -------------------------- | ------------------------------ |
| `K8_contrib_x1`   | **Стажёр**    | 💧  | 1 одобренный ручной релиз. | первый рейс курьера            |
| `K9_contrib_x5`   | **Поставщик** | 🔵  | 5 одобренных релизов.      | подтверждённый канал поставки  |
| `K10_contrib_x20` | **Да, шеф!**  | 🌸  | 20 одобренных релизов.     | несёшь башню коробок, Yes Chef |


**Трек 2 — Твои записи хотят другие (витрина → спрос).** Пластинки из твоей коллекции в чужих вишлистах.


| Код              | Название         | Тир | Условие                               | Образ пина                          |
| ---------------- | ---------------- | --- | ------------------------------------- | ----------------------------------- |
| `K14_wanted_x1`  | **Разыскивается** | 🔵  | 1 твоя пластинка в вишлисте ≥3 людей. | плакат «WANTED», награда за поимку |
| `K15_wanted_x5`  | **Шоурум**       | 🌸  | 5 твоих пластинок хотят другие.       | твоя полка — выставочный зал        |
| `K16_wanted_x10` | **Личный Санта** | 🌸  | 10 твоих пластинок хотят другие.      | у тебя список желаний целого города |


> META для раздела пока нет — добавим `META_contribution`, когда устаканятся пороги на реальной базе.


### 4.9. 🗣 Серия «Глас наружу» (приглашения) ⛔ ОТКЛЮЧЕНА

> **Статус:** серия не показывается в приложении и не приносит XP — реферальной программы нет, ачивки недостижимы. Определения остаются в коде как каркас (все эвалюаторы — `_stub`), чтобы включить одним движением, когда появятся приглашения.
>
> Как именно выключено:
> - `_group_series` в [achievements.py](../../Backend/app/api/achievements.py) безусловно пропускает `series == "invitations"` — секции нет в каталоге ни в обычной выдаче, ни под `include_hidden`;
> - `_EXCLUDED_SERIES` в [levels.py](../../Backend/app/services/achievements/levels.py) исключает серию из XP-счётчика уровня;
> - все эвалюаторы возвращают `EvalResult(unlocked=False)`, так что ачивки не открываются и пуши не летят.
>
> **Чтобы включить:** убрать `continue` по `invitations` в `_group_series`, убрать серию из `_EXCLUDED_SERIES`, заменить `_stub` на реальные эвалюаторы. Пины не нужны, пока серия выключена.
>
> **Требует фичи реферальной атрибуции:** на ссылке публичного профиля параметр `?ref=<username>`, deferred deep link в Mobile (Branch / Firebase Dynamic Links), поле `User.referred_by_user_id`.


| Код                 | Название       | Тир | Условие                                                                                                |
| ------------------- | -------------- | --- | ------------------------------------------------------------------------------------------------------ |
| `INV_first`         | **Сарафан**    | 💧  | Кто-то зарегистрировался по твоей ссылке.                                                              |
| `INV_three`         | **Расходится** | 🔵  | 3 регистрации.                                                                                         |
| `INV_ten`           | **Тренд**      | 🌸  | 10 регистраций.                                                                                        |
| `INV_active_circle` | **Живой круг** | 🌸  | ≥5 приведённых активны ≥30 дней и имеют ≥10 пластинок.                                                 |
| `INV_chain`         | **Цепочка**    | 🌌  | Кто-то, кого ты привёл, сам кого-то привёл.                                                            |
| `INV_from_showcase` | **Из витрины** | 🔵  | По твоей ссылке кто-то добавил пластинку из «Витрины новинок» в первые 7 дней. *Требует фичи витрины.* |
| `META_evangelist`   | **Эпидемия**   | ⚫   | INV_ten + INV_active_circle + INV_chain. Награда: title «Эпидемиолог» + холографическая рамка.         |


### 4.10. 🎼 Серия «Полная дискография» (динамическая)

Самая глубокая. Зависит от Discogs `/artists/{id}/releases`. **Тяжёлые проверки — раз в сутки**, не на каждый `collection_item_added`.


| Код                     | Название        | Тир | Условие                                                                               |
| ----------------------- | --------------- | --- | ------------------------------------------------------------------------------------- |
| `H1_artist_x5`          | **Поклонник**   | 💧  | 5 пластинок одного артиста.                                                           |
| `H2_artist_studio_full` | **Полная**      | 🌌  | Все студийные альбомы одного артиста. *Динамическая: `H2:<artist-slug>`.*             |
| `H3_master_pressings_3` | **Сравнил**     | 🌸  | 3+ разных пресса одного мастера.                                                      |
| `H4_master_pressings_5` | **Археолог**    | 🌌  | 5+ разных прессов одного мастера.                                                     |
| `H5_label_x20`          | **Лейбл-фанат** | 🌸  | 20 пластинок одного лейбла (топ-100 anti-trivial). *Динамическая: `H5:<label-slug>`.* |
| `META_depth`            | **Учёный**      | ⚫   | H2 + H4 + H5 (любые). Награда: title с именем артиста + иконка «🎚 Мастер-копии».     |


### 4.11. 🛒 Серия «Рыночный нюх» 📍

Главная вертикаль покупок. Опирается на `OfferClick` (affiliate-переход) и price drawer. **Анти-фарм:** дедуп `OfferClick` по `(user, listing)` + cooldown, иначе фарм перезаходами.


| Код                  | Название            | Тир | Условие                                            |
| -------------------- | ------------------- | --- | -------------------------------------------------- |
| `M1_first_drawer`    | **Прицениться**     | 💧  | Открыл price drawer первый раз.                    |
| `M2_first_click`     | **Первая вылазка**  | 💧  | Первый affiliate-переход в магазин.                |
| `M3_wishlist_hunter` | **Закрыл гештальт** | 🔵  | Affiliate-переход по пластинке из своего вишлиста. |
| `M4_deal_finder`     | **Цена-огонь**      | 🌸  | Нашёл листинг дешевле Discogs-median.              |
| `M5_regular`         | **Завсегдатай**     | 🌸  | 50 affiliate-переходов суммарно.                   |
| `META_market`        | **Рыночный нюх**    | 🔵  | Все M1–M5. Награда: значок-ценник на карточках.    |


### 4.11b. 💰 Под-серия «Стоимость коллекции» 📍

Опирается на `total_estimated_value_rub`. **Пересчёт только на `daily_tick**`, не на каждое добавление.


| Код              | Название            | Тир | Порог (₽)                                                  |
| ---------------- | ------------------- | --- | ---------------------------------------------------------- |
| `MV1_appraised`  | **Оценено**         | 💧  | Первый расчёт стоимости коллекции.                         |
| `MV2_50k`        | **Полтинник**       | 💧  | 50 000                                                     |
| `MV3_100k`       | **Шестизнак**       | 🔵  | 100 000                                                    |
| `MV4_250k`       | **Четверть лимона** | 🌸  | 250 000                                                    |
| `MV5_500k`       | **Сокровищница**    | 🟪  | 500 000                                                    |
| `MV6_million`    | **Миллионер**       | ⚫   | 1 000 000                                                  |
| `MV_crown_jewel` | **Жемчужина**       | 🌸  | Одна пластинка дороже 20 000 ₽ (источник — fun-stat `D6`). |


### 4.12. 🎚 Серии по форматам носителя (FMT / T / CD / BX)

Награждают за коллекционирование не-винила: кассеты, CD, бокс-сеты. Раньше весь каталог крутился вокруг винила — эти серии закрывают пробел.

#### Как корректно учитывать формат

Источник — `Record.format_type` и `Record.format_description` (заполняются из Discogs `formats[0]`, см. [discogs.py:681](../../Backend/app/services/discogs.py:681)).


| Нюанс                              | Что делать                                                                                                                                                                                          |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Бокс-сет редко в `format_type**` | Discogs часто оставляет `name="Vinyl"`/`"CD"`, а `"Box Set"` кладёт descriptor'ом в `format_description`. **Детект бокса = `"box set" in format_description.lower()**`, не только по `format_type`. |
| **CDr ≈ CD**                       | Сворачивать `CDr`, `CD`, `HDCD`, `SACD` в семью **CD**.                                                                                                                                             |
| **Нормализация**                   | `format_type.strip().lower()`, маппинг в `{vinyl, cassette, cd, box_set}`. Прочее (`File`, `DVD`, `Blu-ray`) — игнор для этих серий.                                                                |
| **Кол-во дисков в боксе**          | Из `format_description` («10×Vinyl») или `formats[].qty`. Нужно для random `R_tabletop_giant`.                                                                                                      |
| **Анти-фарм**                      | Как в [scale.py](../../Backend/app/services/achievements/definitions/series/scale.py): считать только **основную** коллекцию + `added_at <= now-24h`.                                               |


#### 🎚 Серия «Форматы» (кросс-формат, мета) 📍


| Код                 | Название                | Тир | Условие                                                             |
| ------------------- | ----------------------- | --- | ------------------------------------------------------------------- |
| `FMT1_beyond_vinyl` 🖼️ | **За пределами винила** | 💧  | Добавил первый не-винил (кассета / CD / бокс).                      |
| `FMT2_multiformat` 🖼️  | **Мультиформат**        | 🔵  | 3 разных типа носителя в коллекции.                                 |
| `FMT3_all_formats` 🖼️  | **Всеформатный**        | 🌸  | Все 4 типа: винил, кассета, CD, бокс.                               |
| `META_formats` 🖼️      | **Без предрассудков**   | 🌌  | FMT1–FMT3 + по ≥10 единиц каждого формата. Награда: тема «Mixtape». |


#### 📼 Серия «Кассеты» (`T*`) 📍


| Код             | Название          | Тир | Условие         |
| --------------- | ----------------- | --- | --------------- |
| `T1_first_tape` 🖼️ | **Перемотка**     | 💧  | Первая кассета. |
| `T2_tapes_x10` 🖼️  | **Микстейп**      | 💧  | 10 кассет.      |
| `T3_tapes_x25` 🖼️  | **Хром и металл** | 🔵  | 25 кассет.      |
| `T4_tapes_x50` 🖼️  | **Эпоха Walkman** | 🌸  | 50 кассет.      |


#### 💿 Серия «Компакт-диски» (`CD*`) 📍


| Код            | Название             | Тир | Условие                    |
| -------------- | -------------------- | --- | -------------------------- |
| `CD1_first_cd` 🖼️ | **Лазер включён**    | 💧  | Первый CD (вкл. CDr/SACD). |
| `CD2_cds_x25` 🖼️  | **Jewel Case**       | 💧  | 25 CD.                     |
| `CD3_cds_x100` 🖼️ | **Серебряная полка** | 🔵  | 100 CD.                    |
| `CD4_cds_x250` 🖼️ | **Ярче павлина**  | 🌸  | 250 CD.                    |


#### 📦 Серия «Бокс-сеты» (`BX*`) 📍

Боксы дорогие/редкие — серия короткая, вес выше. Детект по `format_description`, см. таблицу выше.


| Код             | Название               | Тир | Условие          |
| --------------- | ---------------------- | --- | ---------------- |
| `BX1_first_box` 🖼️ | **Распаковка**         | 🔵  | Первый бокс-сет. |
| `BX2_boxes_x5` 🖼️  | **Полка ломится**      | 🌸  | 5 боксов.        |
| `BX3_boxes_x15` 🖼️ | **Хранилище делюксов** | 🌌  | 15 боксов.       |


---

## 5. Каталог рандомных (34 задумано, 21 в коде)

> Все скрыты. Все 🌸 Редкая или выше. Без подсказок, без счётчиков, без видимости в каталоге.

### 5.1. Числовые шутки


| Код              | Название              | Триггер                                                                                                  |
| ---------------- | --------------------- | -------------------------------------------------------------------------------------------------------- |
| `R_thirty_three` | **Тридцать три**      | Ровно 33 пластинки в коллекции **И** 24 часа без новых добавлений (= юзер остановился, не на пути к 50). |
| `R_sixty_nine` 🖼️   | **Шестьдесят девять** | Ровно 69 уникальных пластинок + 24h cooldown. *Описание: «Nice».*                                        |
| `R_pi` 🖼️           | **Число Пи**          | Ровно 314 пластинок + 24h cooldown.                                                                      |
| `R_palindrome` 🖼️   | **Палиндром**         | Пластинка с годом-палиндромом (1991, 2002, 2112).                                                        |


### 5.2. Времена и совпадения


| Код                 | Название           | Триггер                                                                                  |
| ------------------- | ------------------ | ---------------------------------------------------------------------------------------- |
| `R_time_machine_50` 🖼️ | **Полвека спустя** | Пластинка с `year=Y` добавлена в год `Y+50` (±30 дней).                                  |
| `R_birth_year`      | **Ровесник**       | Год пластинки совпадает с годом рождения юзера. *Требует поля «дата рождения» в `User`.* |
| `R_new_year` 🖼️        | **Первая в году**  | Добавил первую пластинку года с 00:00 до 00:30 1 января.                                 |
| `R_friday_night` 🖼️    | **Пятничный спин** | Добавил пластинку в пятницу с 22:00 до 02:00.                                            |
| `R_leap_day` 🖼️        | **29 февраля**     | Добавил пластинку 29 февраля.                                                            |


### 5.3. Самореферентные


| Код                | Название         | Триггер                                                                                |
| ------------------ | ---------------- | -------------------------------------------------------------------------------------- |
| `R_self_titled`    | **Тёзка**        | Пластинка с `title == artist` (Black Sabbath / Black Sabbath).                         |
| `R_self_aware` 🖼️     | **Самосознание** | `title` содержит `vinyl` / `винил` / `record` / `пластинка` / `analog`.                |
| `R_meta_vertushka` 🖼️ | **Вертушка**     | `title` или `artist` содержит `turntable` / `вертушк` / `gramophon` / `проигрыватель`. |
| `R_long_title` 🖼️     | **Поэма**        | Пластинка с `title` длиннее 100 символов.                                              |


### 5.3b. Форматы (скрытые, кросс-формат)


| Код                | Название            | Триггер                                                     |
| ------------------ | ------------------- | ----------------------------------------------------------- |
| `R_three_lives` 🖼️    | **Три жизни**       | Один и тот же релиз (master) есть на виниле, CD и кассете.  |
| `R_tapehead` 🖼️       | **Тру-кассетник**   | Кассет в коллекции больше, чем винила (при ≥10 кассет).     |
| `R_cd_renaissance` 🖼️ | **Ренессанс CD**    | 50+ CD при наличии винила — CD не вторичны.                 |
| `R_tabletop_giant` 🖼️ | **Гигант на столе** | Бокс-сет с ≥10 дисками внутри (`format_description`/`qty`). |
| `R_type_iv` 🖼️        | **Type IV**         | Кассета с пометкой Metal / Type IV в `format_description`.  |
| `R_hidden_track` 🖼️ | **Спрятанный трек** | Трек без номера позиции в треклисте либо `hidden`/`untitled` в названии. ⚠️ Отступление от плана: «число треков > заявленного» проверить нечем — Discogs отдаёт только сам треклист, без заявленного количества. Ненумерованный трек — та же конфигурация, что у ранних прессов со спрятанным бонусом. |
| `R_limited_box` 🖼️    | **Лимитка**         | Бокс-сет с `limited`/`numbered`/`anniversary` в описании.   |


### 5.4. Платформенные (отложены до публичного запуска)

Не выкатываем до момента, когда есть смысл говорить «первая сотня/тысяча». Помечаем как hidden в каталоге, но evaluator выключен флагом `RANDOM_PLATFORM_ENABLED=False` до запуска.


| Код                | Название          | Триггер                                                  |
| ------------------ | ----------------- | -------------------------------------------------------- |
| `R_first_thousand` | **Первая тысяча** | `User.id` в первой 1000 зарегистрированных.              |
| `R_first_hundred`  | **Первая сотня**  | В первой 100. ⚫ Легенда.                                 |


### 5.5. Что выкинули

- **`R_completionist`** («Завершитель», 30+ других ачивок). Мета-ачивка за ачивки — награда за то, что уже награждено.

- **Зеркала** (`R_mirror`, `R_double_copy` — пластинка в коллекции и вишлисте / в двух коллекциях). Это поведенческий баг, а не пасхалка — не должно «награждаться».
- **Социальные синхронии** (`R_synchronicity`, `R_mentor`, `R_disciple`). Слишком сложная агрегация по платформе, отложено.

---

## 5.6. Архетипы коллекционера ✅ Заменено на V3 «Физика звука»

> 📍 Источник правды: [PLAN_ACHIEVEMENTS_ARCHETYPES_V3.md](PLAN_ACHIEVEMENTS_ARCHETYPES_V3.md).
> Predicate-каталог ниже (приоритет по мета-кодам — «Эпидемиолог», «Учёный»,
> «Архивист»...) **удалён целиком**, замена — pure XP-ladder.

**Идея.** Никаких predicate-правил по конкретным мета, никаких
breadth/balance gates, никаких overlay-специализаций. Каждая открытая
ачивка даёт очки по тиру (`simple=1, notable=3, rare=10, epic=30,
legend=100` — геометрия ×3). Сумма очков → один из 10 уровней «Физика
звука», от **Тишь** (0) до **Первозвук** (1050, max-score под текущий
каталог — 1105). Юзер копит очки откуда угодно — любая ачивка приближает
следующий уровень, нет «тупиковых» серий.

**Принципы:**

- Один юзер = один уровень, определяется суммой очков, не паттерном серий.
- `computeArchetype` никогда не возвращает null — у любого юзера есть как
  минимум «Тишь».
- Архетип **не косметика** — живёт рядом с косметикой (рамки/темы), но не
  отнимает её.

**Лестница уровней:**


| # | key | label | threshold | flavor |
| --- | --- | --- | ---: | --- |
| 0 | `silence` | **Тишь** | 0 | Ты ещё не нажал на play. Но уже пришёл. |
| 1 | `rustle` | **Шорох** | 10 | Игла коснулась. Всё остальное — дело времени. |
| 2 | `echo` | **Эхо** | 30 | Что-то услышанное однажды не уходит. Оно возвращается. |
| 3 | `wave` | **Волна** | 75 | Ты больше не слушаешь музыку. Ты в ней. |
| 4 | `resonance` | **Резонанс** | 150 | Правильная пластинка в правильный момент — это физика, не случайность. |
| 5 | `overtone` | **Обертон** | 275 | Слышишь то, чего нет в нотах. Значит, слух уже другой. |
| 6 | `amplitude` | **Амплитуда** | 450 | Твоя коллекция давит на воздух. Это чувствуют все, кто входит в комнату. |
| 7 | `frequency` | **Частота** | 650 | Ты настроен точнее большинства. Фальшь слышна за три такта. |
| 8 | `tuning_fork` | **Камертон** | 850 | К тебе приходят сверяться. Ты — точка отсчёта. |
| 9 | `primal_sound` | **Первозвук** | 1050 | Было время до тебя. Теперь от тебя считают. |


**API.** Считается на клиенте из `/api/achievements/me` — `Mobile/lib/archetype.ts`
(`computeScore`, `computeArchetype`). Никаких новых полей на бэкенде не нужно.

**Отображение:** `AchievementsHero` (большой counter + chip «УРОВЕНЬ N/10 ·
Метка» + флейвор-текст + прогресс-бар до следующего порога) и
`ArchetypeChip` (компактный chip с цветной обводкой по тиру уровня) в:

- `Mobile/app/profile.tsx`
- `Mobile/app/user/[username]/index.tsx`
- `app/web/templates/public_profile.html`

**Удалённые ключи** (больше не существуют в `archetype.key`): `evangelist`,
`scientist`, `archivist`, `resident`, `polymath`, `cartographer`,
`eras_keeper`, `grail_hunter`, `selecta`, `gifter`, `searcher`,
`quiet_collector`, `melomane`, `rookie`. `ArchetypeChip.hideRookie` теперь
сравнивает `key === 'silence'`.

---

## 5.7. Скрытая дорожка 🎯 (пасхалки взаимодействия)

> Реакция на жесты внутри приложения + курьёзы коллекции. Все скрытые, 🌸 Редкая или выше, без счётчиков и подсказок. Префикс `E`_ (egg), чтобы не путать с серией H (дискография).
>
> ✅ **Реализована.** Определения — [eggs_interaction.py](../../Backend/app/services/achievements/definitions/eggs_interaction.py), тесты — [test_achievement_eggs_interaction.py](../../Backend/tests/test_achievement_eggs_interaction.py), клиентские счётчики — [eggTracker.ts](../../Mobile/lib/eggTracker.ts).
>
> Половина жестов в БД следов не оставляет (тапы по спиннеру, pull-to-refresh, промах распознавания) — их считает клиент и присылает готовый факт через `POST /achievements/events`, allow-list `CLIENT_EVENTS`. Состояние между событиями (стрик сканов, времена смены аватара, циклы add/remove) живёт в `UserAchievement.ach_metadata`; ради этого `_persist` умеет писать metadata и для ещё не открытой ачивки.
>
> Цвет винила берётся из `Record.discogs_data->>'vinyl_color_raw'` через `color_family()` — отдельной колонки под цвет нет, а сырая строка с Discogs грязная («Red Translucent», «180 Gram», «Gatefold»).


| Код                 | Название               | Тир | Триггер | Как считается |
| ------------------- | ---------------------- | --- | ------- | ------------- |
| `E_glass_eye` 🖼️       | **Глаз-алмаз**         | 🌸  | Скан не распознал обложку, а юзер добавил релиз вручную. | Клиент: `scan_miss_manual_add`, окно 30 мин между промахом и ручным добавлением |
| `E_digitizer` 🖼️       | **Оцифровщик**         | 🌸  | 10 пластинок подряд добавлены сканом. | Клиент: `scan_added`; любое добавление НЕ сканом обнуляет стрик в metadata |
| `E_glow` 🖼️            | **Светится в темноте** | 🌸  | Добавил glow-in-the-dark винил. | Сервер: `vinyl_color_raw ILIKE '%glow%'` |
| `E_spin` 🖼️            | **Закрутил**           | 🌸  | Тапнул вращающийся винил 33 раза на одной карточке. | Клиент: `vinyl_spun_33`; счёт сбрасывается при смене карточки |
| `E_rainbow` 🖼️         | **Радуга**             | 🌸  | 6 цветных винилов разных цветов. | Сервер: 6 разных `color_family()`, чёрный не в счёт |
| `E_second_thoughts` 🖼️ | **Сомнения**           | 🌸  | Добавил и удалил одну и ту же пластинку 3 раза. | Сервер: `collection_item_removed`, счётчик по `record_id` в metadata |
| `E_photo_shy` 🖼️       | **Не та фотка**        | 🌸  | Сменил аватар 5 раз за сутки. | Сервер: `avatar_set` + скользящее окно 24ч по таймстемпам в metadata |
| `E_anniversary` 🖼️     | **Год спустя**         | 🌌  | Зашёл в «Ачивки» в годовщину регистрации (±1 день). | Клиент: `achievements_opened`; сервер сверяет день/месяц с `created_at` |
| `E_pull_78` 🖼️         | **Заело**              | 🌸  | 78 pull-to-refresh за сессию. Отсылка к 78 RPM. | Клиент: `pulled_78`, счётчик в памяти сессии |


---

## 5.8. Пасхалки Маркета 🛒🎯

> Скрытые, 🌸+. Привязаны к разделу «Маркет».


| Код                 | Название              | Триггер                                               |
| ------------------- | --------------------- | ----------------------------------------------------- |
| `MX_night_crate` 🖼️    | **Ночной диггинг**    | Сессия в Маркете между 03:00 и 04:00.                 |
| `MX_window_shopper` | **Только посмотреть** | Открыл price drawer и закрыл без клика 10 раз подряд. |


---

## 6. Награды

Только косметика. Никаких premium-фич, валют, шопа.


| Тип награды           | Где применяется                                      | Откуда выдаётся                                                |
| --------------------- | ---------------------------------------------------- | -------------------------------------------------------------- |
| **Эмалевый пин**      | Раздел «Ачивки», превью на профилях (топ-3 свежих)   | Каждая ачивка                                                  |
| **Рамка карточки**    | Карточки записей в коллекции                         | Мета-ачивки серий (бронза → серебро → золото → платина → холо) |
| **Тема приложения**   | Альтернативный визуал UI                             | META_geography → «Globus», META_eras → «Винтаж»                |
| **Title**             | Под ником в публичном и in-app профиле, в share-card | META_foundation, META_genres, META_evangelist, META_depth      |
| **Иконка коллекции**  | Кастомная иконка пользовательской коллекции          | META_gifts → «Box», META_depth → «🎚 Мастер-копии»             |
| **Анимированный пин** | 3D-вращение в разделе «Ачивки»                       | Только Легенда и мета-ачивки серий                             |


**Принцип:** одна Легенда = одна уникальная косметическая вещь. Косметика **не дублируется** между ачивками — каждая привязана к конкретной истории.

---

## 7. Технический каркас

### 7.1. Модель данных

```python
class UserAchievement(Base):
    __tablename__ = "user_achievements"
    id = Column(UUID, primary_key=True, default=uuid4)
    user_id = Column(UUID, ForeignKey("users.id", ondelete="CASCADE"), nullable=False, index=True)
    code = Column(String(64), nullable=False, index=True)  # 'C2_limited_x25' или 'H2:king-crimson'
    unlocked_at = Column(DateTime(timezone=True), nullable=False, default=now)
    progress = Column(Integer, default=0, nullable=False)
    progress_target = Column(Integer, default=0, nullable=False)
    is_unlocked = Column(Boolean, default=False, nullable=False, index=True)
    metadata = Column(JSONB, nullable=True)  # для H2/H5 — имя артиста/лейбла

    __table_args__ = (UniqueConstraint("user_id", "code"),)
```

### 7.2. Расположение кода

```
Backend/app/services/achievements/
├── registry.py              # каталог ачивок
├── evaluator.py             # ядро
├── share_card.py            # генерация PNG для шеринга
├── definitions/
│   ├── series/              # серии по файлам
│   │   ├── foundation.py
│   │   ├── scale.py
│   │   ├── rarity.py
│   │   ├── geography.py
│   │   ├── eras.py
│   │   ├── genres.py
│   │   ├── gifts.py
│   │   ├── community.py
│   │   ├── invitations.py
│   │   └── discography.py
│   └── random.py
└── tasks.py                 # фоновые (раз в сутки — H, INV_active_circle, R_thirty_three cooldown)
```

### 7.3. События

Эмитятся из API-обработчиков в той же транзакции, обрабатываются evaluator'ом синхронно. Дорогие проверки (H-категория) идут в `daily_tick`.

Ключевые события: `collection_item_added`, `wishlist_item_added`, `gift_booked`, `gift_completed`, `gift_received`, `follow_created`, `follow_received`, `profile_view` (только если viewer ≠ owner), `profile_shared_enabled`, `avatar_set`, `user_registered_via_ref`, `daily_tick`.

### 7.4. Идемпотентность

- `unlock_achievement` — upsert по `(user_id, code)`. Двойной event = один анлок.
- `update_progress` — пишем только если новое значение `>` текущего.
- Evaluator'ы — pure, без side-effects.

### 7.5. Анти-фарм


| Угроза                                    | Защита                                                            |
| ----------------------------------------- | ----------------------------------------------------------------- |
| Накачал 1000 пластинок и удалил → B-серия | `COUNT(DISTINCT record_id)` от записей старше 24 часов.           |
| Подарки циклом «друг ↔ друг» → J3/J4      | J3/J4 считают **разных получателей**.                             |
| Накачал followers ботами → K              | Фолловер должен иметь ≥10 пластинок и аккаунт ≥30 дней.           |
| Просмотры профиля сам себе → K5/K6        | View не инкрементится, если `viewer_user_id == owner_id`.         |
| Подбор «ровно 33» → R_thirty_three        | 24h cooldown без новых добавлений.                                |
| Накрутка приглашений → INV                | Приглашённый должен иметь ≥10 пластинок и быть активным ≥30 дней. |


---

## 8. UI: раздел «Ачивки»

### 8.1. На своём профиле (`Mobile/app/profile.tsx`)

Большой visual focal point под аватаркой/основной шапкой.

```
┌─ Профиль ──────────────────────────────┐
│ [Аватар]  @username                    │
│           «Эрудит»                     │
│                                        │
│  ┌─ Ачивки ──────────  открыто 17 / 65 │
│  │  [пин] [пин] [пин]  [→ все]         │
│  │                                     │
│  └─────────────────────────────────────│
│                                        │
│  Коллекции ...                         │
└────────────────────────────────────────┘
```

При тапе на блок «Ачивки» → экран `Mobile/app/achievements.tsx`:

```
┌─ Ачивки ───────────────  открыто 17 / 65 │
│                                          │
│  Серии:                                  │
│  ┌────────────────────────────────────┐  │
│  │ 🌱 Первые шаги        5 / 5 ✅     │  │
│  │ 📚 Размер коллекции   2 / 6 ▓▓░░░ │  │
│  │ 💎 Охота за редкостями 1 / 7 ▓░░ │  │
│  │ ...                                │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ❓ Сюрпризы               открыто 3     │
│     [нажми чтобы посмотреть найденные]   │
│                                          │
│  Недавно открыто:                        │
│  • 10 мая  •  R_self_titled  «Тёзка»     │
│  • 8 мая   •  B2             «Полтинник» │
└──────────────────────────────────────────┘
```

При тапе на серию → детальный экран с гридом пинов, фильтрами и описаниями.

### 8.2. На чужом профиле в приложении (`Mobile/app/user/[username].tsx`)

Аналогичный блок «🏆 Ачивки» с превью топ-3 свежих + счётчик «12 ачивок». Тап → грид всех ачивок этого юзера (read-only).

### 8.3. На публичном web-профиле (`ProfileShare`)

Компактный блок «Ачивки (17)» с превью топ-3 свежих. Без полного грида — web-страница и так длинная.

### 8.4. Шеринг в Instagram Stories

При тапе на конкретный пин в гриде / в анлок-анимации → кнопка «Поделиться».

**Генерация:** `POST /api/achievements/me/share-card/{code}` → возвращает URL PNG `1080×1920` (Stories 9:16).

**Композиция картинки:**

```
┌─────────────────────────┐  ← 1080×1920
│   градиентный фон       │
│   (цвет тира)           │
│                         │
│   [крупный пин 700×700] │  ← центр
│                         │
│   «Тёзка»               │  ← название, белый, 80px
│   Редкая · 10 мая 2026  │  ← тир + дата, 36px
│                         │
│   [аватар] @username    │  ← нижний блок, ~120 от низа
│   vinyl-vertushka.ru    │  ← логотип/ссылка, мелко
└─────────────────────────┘
```

**Автокроп под другие форматы:**

- 1080×1080 (Instagram Feed) — обрезаем 420 px сверху и снизу.
- 1080×1350 (Instagram Portrait) — обрезаем 285 px сверху и снизу.
- 1080×1920 (Telegram Stories, TikTok) — оригинал.

**Бэкенд:** Pillow + предзагруженные SVG-пины через `cairosvg`. Кэш по `code` (картинка не зависит от user-данных, кроме нижнего блока — отдельный композитинг username/avatar поверх).

**Mobile:** после генерации показываем `react-native-view-shot` overlay с превью + Share Sheet (iOS/Android share API → Instagram, Telegram, прочее).

### 8.5. Анимация анлока

Поверх экрана: затемнение → пин падает на проигрыватель → пин крутится 360° → разворачивается лента с названием + тиром. 2.2 сек до интерактивной фазы. Кнопки: «Поделиться» / «Закрыть».

**Правило батчинга:** в одной серии (`B`*, `J*`, …) при батч-анлоке показываем только самый высокий тир, остальные начисляем молча с пометкой «и ещё 3 на пути к META_scale».

### 8.6. Push

- Только в момент анлока, не чаще 1 push в час (батчинг).
- Рандомные ачивки — без push, только всплывающий тост в приложении («Ого, ты нашёл сюрприз»). Сохраняем эффект «не может быть».

---

## 9. Фазы внедрения


| Фаза                            | Длительность | Содержимое                                                                                                                           | Зависимости                       | Детально |
| ------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------- | -------- |
| **Phase 0 — Test mode**         | 1 неделя     | 10 ачивок. Каркас registry/evaluator/UI.                                                                                             | —                                 | §10      |
| **Phase 1 — Foundation core**   | 2 недели     | Полные серии 🌱 + 📚 + 👥. Раздел «Ачивки» с гридом. Рандомные §5.1 и §5.3. Share-card инфраструктура.                               | Дизайн пинов готов                | —        |
| **Phase 2 — Social + Gifting**  | 2 недели     | Серии 🎁 + 🗣. Поле «дата рождения» в `User`. R_birth_year, R_new_year, R_friday_night, R_leap_day, R_palindrome, R_time_machine_50. | INV требует реферальной атрибуции | §11      |
| **Phase 3 — Депт и культура**   | 3 недели     | Серии 💎 + 🌍 + 📅 + 🎼. Темы «Globus», «Винтаж».                                                                                    | 💎 требует `RARITY_BADGES_PLAN`   | §12      |
| **Phase 4 — Дискография**       | 3 недели     | Серия 🎼 (полная дискография) + daily_tick инфраструктура.                                                                           | Стабильный Discogs API кэш        | §13      |
| **Phase 5 — Древо и косметика** | 2 недели     | Полноценное древо серий в UI. Анимированные пины для Легенда. Активация R_first_thousand/R_first_hundred при достижении базы.        | UI/анимации                       | §14      |


**До релиза полного каталога:** 13 недель.

---

## 10. Phase 0 — Test mode (детально)

Цель: задеплоить минимальный каркас, проверить event → evaluator → unlock → UI → push pipeline на реальных пользователях.

### 10.1. Каталог Phase 0 (10 ачивок)


| Код                 | Название         | Серия / Рандом     | Что тестирует                                              |
| ------------------- | ---------------- | ------------------ | ---------------------------------------------------------- |
| `A1_first_record`   | **Поехали**      | Первые шаги        | Эмиссия `collection_item_added`.                           |
| `A2_first_wishlist` | **Хотелка**      | Первые шаги        | Эмиссия `wishlist_item_added`.                             |
| `A3_avatar_set`     | **Аватар**       | Первые шаги        | Эмиссия `avatar_set`.                                      |
| `A4_public_profile` | **Распахнул**    | Первые шаги        | Эмиссия `profile_shared_enabled`.                          |
| `META_foundation`   | **На борту**     | Первые шаги (мета) | Логика мета-ачивки: «все из серии».                        |
| `B1_starter`        | **Десятка**      | Размер коллекции   | Логика порога с DISTINCT и 24h cooldown.                   |
| `B2_collector`      | **Полтинник**    | Размер коллекции   | Тот же триггер, разные пороги.                             |
| `J1_first_gift`     | **Подарил** ⚠️ переименована в «Забронировал», см. §4.7 | Дарящая рука | Эмиссия `gift_booked`. |
| `R_self_titled`     | **Тёзка**        | Рандом             | Скрытая ачивка без UI-подсказки. Логика `title == artist`. |
| `R_thirty_three`    | **Тридцать три** | Рандом             | 24h cooldown механика — самое сложное в Phase 0.           |


### 10.2. Бэкенд (acceptance criteria)

- Миграция: таблица `user_achievements` создана, индексы на `(user_id, is_unlocked)` и `(user_id, code)`.
- `Backend/app/services/achievements/` — каркас: `registry.py`, `evaluator.py`, `definitions/series/foundation.py`, `definitions/series/scale.py`, `definitions/series/gifts.py`, `definitions/random.py`.
- Эмиссия событий в `Backend/app/api/`:
  - `collections.py` после создания `CollectionItem` → `collection_item_added`
  - `wishlists.py` после создания `WishlistItem` → `wishlist_item_added`
  - `auth.py`/`users.py` при обновлении `avatar_url` → `avatar_set`
  - Эндпоинт активации публичного профиля → `profile_shared_enabled`
  - `gifts.py` после `GiftBooking` со status=booked → `gift_booked`
- Роутер `Backend/app/api/achievements.py`:
  - `GET /api/achievements/me` — список ачивок текущего юзера.
  - `GET /api/achievements/by-username/{username}` — публично-видимые ачивки.
  - `GET /api/achievements/catalog` — каталог видимых серий. Рандомные не возвращаются.
  - `GET /api/achievements/me/random` — только полученные рандомные.
- Идемпотентность: интеграционный тест — 10 эмиссий одного события = ровно 1 анлок.
- Анти-фарм B-серии: интеграционный тест — добавил 10 пластинок и удалил 5 → B1 не выдаётся (нужны >24h).

### 10.3. Mobile (acceptance criteria)

- Экран `Mobile/app/achievements.tsx` с гридом серий + блоком рандомных.
- Блок «Ачивки» в `Mobile/app/profile.tsx` с превью топ-3.
- Блок «Ачивки» в `Mobile/app/user/[username].tsx` с превью топ-3.
- 10 SVG-пинов в `Mobile/assets/achievements/` (рисует дизайнер по §3.2).
- Компонент `AchievementUnlockOverlay` — анимация падающего пина, 2.2 сек.
- Push-уведомление при анлоке (через существующий push-канал).
- Рандомные ачивки: всплывающий тост, без push.
- Шеринг — пока без share-card-генерации, заглушка «скоро». Реальная генерация — Phase 1.

### 10.4. Тестовые сценарии для ручной проверки

1. Регистрация → добавление пластинки → анлок A1 «Поехали».
2. Создание вишлиста с одной записью → A2 «Хотелка».
3. Установка аватара → A3 «Аватар».
4. Активация публичного профиля → A4 «Распахнул». Если A1–A3 получены, дополнительно META_foundation «На борту».
5. Добавление 10 пластинок (разные `record_id`) → B1 «Десятка» **через 24 часа**.
6. Добавление пластинки с `title=="Black Sabbath"` и `artist=="Black Sabbath"` → R_self_titled «Тёзка», тост без push.
7. Добавление 33-й пластинки → ничего сразу. Через 24h без новых → R_thirty_three анлокается фоновым `daily_tick`.

### 10.5. Что НЕ делаем в Phase 0

- Никаких рамок, тем, иконок коллекций (Phase 5).
- Никакого древа — только списочный грид.
- Никакой share-card генерации (Phase 1).
- Никаких фоновых задач, кроме `daily_tick_random_cooldown` для R_thirty_three.

---

## 11. Phase 2 — Social + Gifting (детально)

Цель: достроить серии 🎁 + 🗣 и шесть «временны́х» рандомов. Требует фичи
реферальной атрибуции и поля «дата рождения» в `User`.

### 11.1. Каталог Phase 2


| Код                   | Название         | Серия / Рандом      | Заметки                                                                   |
| --------------------- | ---------------- | ------------------- | -------------------------------------------------------------------------- |
| `J2_gift_done`        | **Долетело** ✅    | Дарящая рука        | Эмиссия `gift_completed`.                                                  |
| `J3_three_recipients` | **Дарящая рука** ✅ | Дарящая рука      | Анти-фарм: разные `recipient_user_id`.                                     |
| `J4_ten_recipients` 🖼️   | **Праздник** ✅    | Дарящая рука        | —                                                                          |
| `J5_first_received` 🖼️   | **С теплом** ✅    | Дарящая рука        | Эмиссия `gift_received`.                                                   |
| `J7_boomerang` 🖼️        | **Бумеранг** ✅    | Дарящая рука        | Подарил тому, кто раньше дарил тебе.                                       |
| `J8_loved` 🖼️            | **Любимчик** ✅    | Дарящая рука        | Получил подарки от 3 разных дарителей.                                     |
| `J9_santa` 🖼️            | **Дед Мороз** ✅   | Дарящая рука        | Окно 25.12–14.01 по `completed_at`.                                        |
| `META_gifts` 🖼️          | **Щедрость** ✅    | Дарящая рука (мета) | Ядро серии: J2 + J3 + J4 + J5.                                             |
| `INV_first` 🖼️           | **Сарафан**      | Глас наружу         | Триггер `referred_user_registered`.                      |
| `INV_three` 🖼️           | **Расходится**   | Глас наружу         | —                                                        |
| `INV_ten` 🖼️             | **Тренд**        | Глас наружу         | —                                                        |
| `INV_active_circle` 🖼️   | **Живой круг**   | Глас наружу         | ≥5 приведённых активны ≥30д, ≥10 пластинок.              |
| `INV_chain` 🖼️           | **Цепочка**      | Глас наружу         | Приведённый сам кого-то привёл.                          |
| `INV_from_showcase` 🖼️   | **Из витрины**   | Глас наружу         | Требует фичи «Витрина». Если её нет — пропускаем.        |
| `META_evangelist` 🖼️     | **Эпидемия**     | Глас наружу (мета)  | INV_ten + INV_active_circle + INV_chain.                 |
| `R_birth_year`        | (рандом)         | Рандом §5.2         | Год пластинки == год рождения юзера.                     |
| `R_new_year` 🖼️          | (рандом)         | Рандом §5.2         | Добавление пластинки 31.12 или 01.01.                    |
| `R_friday_night` 🖼️      | (рандом)         | Рандом §5.2         | Добавление 5 пластинок подряд в пт после 22:00 локально. |
| `R_leap_day` 🖼️          | (рандом)         | Рандом §5.2         | Добавление 29 февраля.                                   |
| `R_palindrome` 🖼️        | (рандом)         | Рандом §5.1         | Год пластинки — палиндром.                               |
| `R_time_machine_50` 🖼️   | (рандом)         | Рандом §5.2         | Год пластинки на ≥50 лет раньше года рождения.           |


### 11.2. Бэкенд (acceptance criteria)

- Поле `User.birth_date: date | None` (миграция, nullable). Ввод — отдельный
экран онбординга/настроек, не блокирующий регистрацию.
- Реферальная атрибуция:
  - Параметр `?ref=<username>` на `/share/{username}` (web).
  - Deferred deep link в Mobile (Branch / Firebase Dynamic Links — решение
  в §15, открытый вопрос #3).
  - Поле `User.referred_by_user_id: UUID | None`.
  - Эмиссия `referred_user_registered` в `Backend/app/api/auth.py` сразу
  после успешной регистрации с заполненным `referred_by_user_id`.
- ✅ Эмиссия `gift_completed`/`gift_received` реализована через
`emit_gift_completion_events()` в `Backend/app/services/gifts.py`, зовётся
ПОСЛЕ финального commit из ОБОИХ путей завершения брони:
`PUT /api/gifts/me/received/{id}/complete` и
`POST /api/wishlists/items/{id}/move-to-collection`. `recipient_user_id`
денормализован на `GiftBooking` (миграция `20260623_gift_recipient`,
заполняется на `/book` и в `complete_gift_booking`) — без него связь
получатель↔бронь терялась после COMPLETED (`wishlist_item_id` обнуляется).
- Daily-job `daily_tick_referrals`:
  - Проверяет приведённых юзеров на активность (≥10 пластинок, возраст ≥30д).
  - При достижении эмитит `referred_user_activated` для пригласившего.
  - Эмитит `daily_tick` для перерасчёта INV_active_circle / INV_chain.
- Включить реальные evaluator-ы в:
  - ✅ `Backend/app/services/achievements/definitions/series/gifts.py` —
  `_stub` заменён на реальные функции для J2–J5 + J7/J8/J9 + META_gifts.
  J6 удалена целиком (см. §4.7).
  - `Backend/app/services/achievements/definitions/series/invitations.py` —
  реальные функции для INV_* + META_evangelist.
  - `Backend/app/services/achievements/definitions/random.py` — добавить
  6 новых рандомов с эвристиками из §5.1/5.2.
- Интеграционный тест: цепочка `referred_user_registered` × 3 → INV_three.
- Интеграционный тест: завершённый `gift_completed` с `recipient_a`,
`recipient_a`, `recipient_b` (один и тот же получатель дважды) → J3 не
выдаётся, нужны 3 *разных*.
- ⚠️ Найден и пофикшен попутный баг: `move_to_collection` не эмитил
`COLLECTION_ITEM_ADDED` ни в одной из веток — перенос вишлист→коллекция не
открывал C-серию (редкости), scale, genres, eras, geo. Прямое добавление в
коллекцию эмитило исправно, перенос — нет. Emit добавлен после общего
блока if/else в `Backend/app/api/wishlists.py`.

### 11.3. Mobile (acceptance criteria)

- Онбординг-шаг «День рождения» — необязательное поле, ссылка «пропустить».
- Экран настроек профиля: редактирование `birth_date`.
- При шеринге публичного профиля автоматически добавлять `?ref=<username>`.
- При первом запуске после установки — проверка deferred deep link и
запись `referred_by_user_id` в реквест регистрации.
- SVG-пины: `j2_gift_done`, `j3_three_recipients`,
`j4_ten_recipients`, `j5_first_received`, `j7_boomerang`, `j8_loved`,
`j9_santa`, `meta_gifts` — ⚠️ дизайн ещё не готов, в Mobile временно
`SceneGiftBox` как заглушка. Плюс (Phase 2, не задеплоено): `inv_first`,
`inv_three`, `inv_ten`, `inv_active_circle`, `inv_chain`,
`inv_from_showcase`, `meta_evangelist` — заходят в
`Mobile/components/achievement-scenes/index.tsx` через `REGISTRY`,
`SceneDefault` остаётся fallback.
- Анлок-анимация серии J/INV не отличается от Phase 0 (тот же
`AchievementUnlockOverlay`).

### 11.4. Тестовые сценарии для ручной проверки

1. Юзер B регистрируется по ссылке юзера A → у A анлок `INV_first` «Сарафан».
2. Повторить шаг 1 с двумя новыми юзерами → `INV_three` «Расходится».
3. Юзер A завершает 3 подарка трём разным получателям → `J3_three_recipients`
  - `J2_gift_done` (если ещё не было).
4. Юзер A завершает 4-й подарок тому же `recipient_a` из шага 3 → счётчик J4 не
  двинулся, ачивка не открылась.
5. После J2+J3+J4+J5 закрыты → `META_gifts` «Щедрость» (ядро серии, J6
  удалена — см. §4.7). Юзер A дарит тому, кто раньше дарил ему → `J7_boomerang`.
  Юзер получает подарки от 3 разных дарителей → `J8_loved`. Подарок дошёл
  25.12–14.01 → `J9_santa` «Дед Мороз».
6. У 5 приведённых юзеров стали ≥10 пластинок и аккаунту ≥30 дней →
  `daily_tick_referrals` эмитит `referred_user_activated` → `INV_active_circle`.
7. Юзер B (приведённый юзером A) сам привёл юзера C → у A анлок `INV_chain`.
  Затем INV_ten + INV_active_circle + INV_chain → `META_evangelist` «Эпидемия».
8. Юзер с `birth_date=1989-XX-XX` добавляет пластинку `year=1989` → рандом
  `R_birth_year`, тост без push.
9. Добавление любой пластинки 31.12.YYYY → `R_new_year`.
10. Добавление 5 пластинок подряд в пятницу с 22:00 до 23:59 локально →
  `R_friday_night`.
11. Добавление пластинки 29.02 → `R_leap_day` (только в високосный год).
12. Добавление пластинки с годом-палиндромом (1991, 2002, 2112) → `R_palindrome`.

### 11.5. Что НЕ делаем в Phase 2

- Не показываем «дерево рефералов» — это Phase 5 / отдельная фича.
- Не делаем UI «друзья получили эту ачивку» (отвергнуто пользователем).
- Не считаем INV_from_showcase до выкатки фичи «Витрина».

---

## 12. Phase 3 — Депт и культура (детально)

Цель: четыре длинные коллекционерские серии — Rarity, Geography, Eras, Genres.
Требует `[RARITY_BADGES_PLAN.md](RARITY_BADGES_PLAN.md)` для серии 💎.

### 12.1. Каталог Phase 3

См. §4.3 (C-серия, 7 ачивок + META_rarity), §4.4 (D-серия, 7 + META_geography),
§4.5 (E-серия, 6 + META_eras), §4.6 (F-серия, 6 + META_genres). Всего: **30
ачивок**, включая 4 META.

### 12.2. Бэкенд (acceptance criteria)

- Зависимость: на `Record` добавлены поля `is_collectible: bool`,
`is_limited: bool`, `is_hot: bool` (см. `RARITY_BADGES_PLAN.md`).
- Зависимость: `Record.country` нормализован (USSR-варианты сведены к
`USSR`, UK-варианты к `UK`, German-варианты к `Germany`/`West Germany`).
- Реальные evaluator-ы в:
  - `definitions/series/rarity.py` — C1–C7 + META_rarity. Анти-фарм 24h как
  в B-серии.
  - `definitions/series/geography.py` — D1–D7 + META_geography. Использует
  DISTINCT по `country`, плюс specific фильтры для D4 (Japan), D5
  (Melodiya / USSR), D6 (UK + is_collectible), D7 (Germany / West Germany).
  - `definitions/series/eras.py` — E1–E6 + META_eras. E4 «Сегодняшний»
  зависит от `func.current_date()` — пересчёт раз в сутки через DAILY_TICK.
  - `definitions/series/genres.py` — F1–F6 + META_genres. F1/F2 считают по
  DISTINCT первого жанра из массива `Record.genre`.
- Расширить `daily_tick_achievements` так, чтобы он перерасчитывал все META
серий Phase 3 (они зависят от композиции уже выданных под-ачивок).
- Интеграционный тест: меняем `Record.is_limited` на false для уже добавленной
пластинки — C1 остаётся (см. §15, открытый вопрос #5).
- Интеграционный тест: добавили 30 пластинок из 30 стран → D3, и после
этого добавили 10 японских → D4, и META_geography только когда D3 + 3
из D4–D7.

### 12.3. Mobile (acceptance criteria)

- SVG-пины для всех 30 кодов C-/D-/E-/F- + 4 META.
- На экране `achievements.tsx` все 4 серии отображаются как отдельные
`SeriesGroup`. Порядок: Foundation → Scale → Rarity → Geography → Eras →
Genres → Gifts → Community → Invitations → Discography.
- Сцены (analog-format motifs) для каждого пина согласно §3.2 — рисуются
дизайнером, регистрируются в `Mobile/components/achievement-scenes/`.
- Награды Phase 3: тема «Globus» (D-META), тема «Винтаж» (E-META), title
«Эрудит» (F-META), голографическая рамка карточек (C-META). Включаются
сразу после анлока соответствующей META; UI применения тем — Phase 5.
- Подложку Hero (`AchievementsHero`) пересчитать на максимальный тир из
этих новых META (Эпическая для C/D/F, Легенда для E_META).

### 12.4. Тестовые сценарии для ручной проверки

1. Добавление 5 пластинок с `is_limited=true` → `C1_limited_x5` через 24 часа
  (анти-фарм).
2. Добавление 1 пластинки с `is_collectible=true` → `C3_collectible_x1` сразу.
3. Добавление 5 пластинок из 5 разных стран → `D1_country_x5`.
4. Добавление 30 пластинок из 30 стран → `D3_country_x30`. После — 10 японских,
  10 Melodiya, 3 UK-коллекционки → `D4`, `D5`, `D6`. На третьем из них —
   `META_geography` «Атлас».
5. Добавление 5 пластинок 1960–1969 → `E1_60s`. Аналогично 70-е, 80-е, modern,
  pre-1960. Без E6 META_eras не выдаётся.
6. Добавление пластинок с разным `genre` (5 уникальных) → `F1_diversity_5`,
  потом до 10 → `F2_diversity_10`. После — 25 Jazz → `F3`. На любом из
   F3–F6 после F2 META_genres «Эрудит» можно проверять.
7. Удаление пластинки из collection после получения C1 → C1 остаётся (по §15.5).

### 12.5. Что НЕ делаем в Phase 3

- Не выдаём C-серию до накатывания RARITY_BADGES_PLAN (флаги на Record).
- Не строим Discogs-зависимости (это Phase 4 / H-серия).
- Не делаем «сюжетные карточки» страны/жанра — Phase 5.

---

## 13. Phase 4 — Дискография (детально)

Цель: серия 🎼 H — самая глубокая. Полностью завязана на Discogs API + daily-job.

### 13.1. Каталог Phase 4

См. §4.10 (H-серия, 5 ачивок + META_depth). Динамические коды:
`H2:<artist-slug>`, `H4:<master-slug>`, `H5:<label-slug>` — `code` в БД
содержит составной ключ, а человеко-читаемое имя живёт в `ach_metadata.label`.

### 13.2. Бэкенд (acceptance criteria)

- Кэш Discogs:
  - `artist_releases(artist_id)` — список студийных альбомов с TTL 7 дней.
  - `master_pressings(master_id)` — список релизов master-а с TTL 14 дней.
  - `label_top100` — топ-100 лейблов по числу релизов, перерасчёт раз в
  квартал. Только эти 20+ ачивки могут открываться (anti-trivial).
- Реальные evaluator-ы в `definitions/series/discography.py`:
  - `H1_artist_x5` — COUNT(DISTINCT record_id) GROUP BY artist_id ≥ 5. На
  `collection_item_added`.
  - `H2_artist_studio_full` — динамическая ачивка. При создании запись в
  `user_achievements` имеет `code='H2:<slug>'` + `ach_metadata={artist_id, artist_name}`. Только в daily-job, не на каждый add.
  - `H3_master_pressings_3`, `H4_master_pressings_5` — аналогично, по
  `Record.master_id`.
  - `H5_label_x20` — динамическая, только если `label_id` ∈ топ-100.
  - `META_depth` — выдаётся когда у юзера есть ≥1 `H2:`*, ≥1 `H4:*`, ≥1
  `H5:*`. Закрывается только ОДНОЙ парой (см. §15, открытый вопрос #6).
- Daily-job `daily_tick_discography`:
  - Идёт по юзерам с ≥5 пластинками одного артиста.
  - Проверяет полноту дискографии через кэш Discogs.
  - Открывает `H2:<slug>` при полном покрытии.
  - Лимит: не более 200 юзеров за тик (rate-limit Discogs).
- Интеграционный тест: моки Discogs возвращают 5 студийных альбомов,
юзер собирает 4 → H2 не выдаётся. Собирает 5-й → H2 в следующем
daily-tick.

### 13.3. Mobile (acceptance criteria)

- SVG-пины для H1–H5 + META_depth.
- Динамические ачивки в UI: pin показывает `artist_name` / `label_name` /
`master_name` из `ach_metadata`. Бэкенд возвращает это поле в
`MyAchievementsResponse.items[].metadata`.
- Анлок-анимация `H2:<slug>` подтягивает имя артиста в title overlay:
«Полная: Pink Floyd».
- Несколько `H2:*` отображаются как одна групповая ячейка с +N счётчиком
(типа «Полная ×3», тап раскрывает список).
- Награда META_depth: title с именем артиста (юзер выбирает, какой H2
приклеить к нику).

### 13.4. Тестовые сценарии для ручной проверки

1. Юзер добавляет 5 разных альбомов Pink Floyd → `H1_artist_x5` сразу.
2. Юзер добавляет все студийные альбомы Pink Floyd (по моку Discogs — 15 шт) →
  `H2:pink-floyd` через следующий daily_tick.
3. Юзер добавляет ещё один артиста с полной дискографией (Joy Division) → у
  юзера 2 экземпляра `H2:*`. В UI — «Полная ×2». META_depth остаётся
   закрыта, потому что нет H4/H5.
4. Юзер добавляет 5 разных прессов мастера «Dark Side of the Moon» →
  `H4:dsotm-master`. Теперь H2 + H4 есть, не хватает H5.
5. Добавляет 20 пластинок лейбла Blue Note (топ-100) → `H5:blue-note`. На
  этом — `META_depth` «Учёный». Можно выбрать в настройках title «Учёный:
   Pink Floyd».
6. Юзер добавляет 20 пластинок какого-то локального лейбла, не из топ-100 →
  H5 не выдаётся.

### 13.5. Что НЕ делаем в Phase 4

- Не делаем «прогресс H2» (3/15) в UI на пинах — дискография длинная,
выглядит дёшево. Только % прогресса в DetailsSheet.
- Не пытаемся определять «студийный vs концертный» для H2 без Discogs
`release_type=Album` — слишком шумно.
- Не выдаём H5 для лейблов вне топ-100 даже если у юзера их 20+.

---

## 14. Phase 5 — Древо и косметика (детально)

Цель: визуальный «зал ачивок», применение наград, активация рандомов на базе
пользователей.

### 14.1. Состав Phase 5

- Полноценное «древо серий» в UI (вместо текущего вертикального грида).
- Применение косметических наград: темы, рамки, title-ы, иконки коллекций.
- Активация рандомов §5.1 «Числовые шутки» (`R_first_hundred`,
`R_first_thousand`) — после того как ≥1000 юзеров в продакшне.
- Анимированные пины для тира Легенда.

### 14.2. Бэкенд (acceptance criteria)

- Эндпоинт `GET /api/users/me/cosmetics` — возвращает доступные
косметические награды (темы, рамки, title-ы) по разлоченным META.
- Эндпоинт `PATCH /api/users/me/cosmetics` — применить тему / выбрать
title / переключить рамку.
- Поля в `User`: `selected_theme: str | None`, `selected_title_code: str | None`, `selected_frame_code: str | None`.
- Активация `R_first_hundred` / `R_first_thousand` — флаг в config-е +
проверка `total_users >= N` в `daily_tick` перед регистрацией ачивки.
- Серверный share-card подхватывает выбранную рамку и фоновую тему юзера.

### 14.3. Mobile (acceptance criteria)

- Замена грида серий в `app/achievements.tsx` на «древо» — серии разложены
по 2D-канвасу с курсами «эпоха → коллекция → социал». Жесты pinch-zoom.
- Экран настроек «Косметика» — выбор темы, title-а, рамки.
- Темы (минимум 4): Globus (D-META), Винтаж (E-META), Hi-Fi (Phase 1 базовая),
Tape (J-META). Меняют primary-цвета приложения.
- Анимированные пины для Легенды: вращение/пульсация sheen, SVG-маска.
Применяется к: `META_eras`, `META_evangelist`, `META_depth`,
`R_first_thousand`.
- Holo-рамка карточек коллекции для C-META — анимированный градиент.
- Title под ником в `profile.tsx` (если выбран) + chip иконки рамки на
аватарке.

### 14.4. Тестовые сценарии для ручной проверки

1. Юзер с META_eras открывает настройки → может выбрать тему «Винтаж»;
  primary-цвета приложения меняются.
2. Юзер с META_depth выбирает title «Учёный: Pink Floyd» → отображается под
  ником на своём и публичном профиле.
3. Юзер с META_rarity видит holo-рамку на карточках лимиток в коллекции.
4. После публикации в сторе и набора ≥100 юзеров — следующий 100-й юзер
  получает `R_first_hundred`. Аналогично 1000.
5. Древо ачивок: pinch-zoom не ломает скролл, тап на пине открывает
  DetailsSheet (как в Phase 0).

### 14.5. Что НЕ делаем в Phase 5

- Не делаем «сезонные» темы или ограниченные по времени награды (отвергнуто
в §1).
- Не делаем «торговать косметикой» — это Phase Monetization, отдельный план.

---

## 15. Открытые вопросы

1. **Имена тиров** — основной набор «Простая → Заметная → Редкая → Эпическая → Легенда» или винил-метафора «Сингл → EP → LP → Box-сет → Тест-прессинг»? Решение нужно до старта дизайна пинов.
2. **Дата рождения юзера** — нужна для `R_birth_year`. Опциональное поле в `User`, ввод в онбординге или настройках. Phase 1.
3. **Реферальная атрибуция** — Branch / Firebase Dynamic Links vs кастомный сервис. До Phase 2.
4. **Видимость рандомных на чужих профилях** — показывать «❓ 7 сюрпризов» без названий? Решение: да, intrigue-эффект.
5. **Удаление пластинки → потеря B-ачивки** — НЕ отзываем. Один раз получил — навсегда.
6. **Дубликаты H-серии** — два артиста с полной дискографией = две `H2:<slug>`. META_depth закрывается одной из них.
7. **Где в `profile.tsx` блок «Ачивки»** — над коллекциями или под? Предлагаю **над**: focal point важнее списка. Уточнить с дизайнером.

---

## 16. Чек-лист на старт Phase 0

- Согласовать каталог Phase 0 (§10.1).
- Дизайнер рисует 10 SVG-пинов (§3.2).
- Согласовать имена тиров (§3.1 vs альтернатива).
- Создать миграцию `user_achievements`.
- Накатить каркас `Backend/app/services/achievements/`.
- Реализовать evaluator'ы для 10 ачивок.
- Эмиссия событий в существующих API-обработчиках.
- Роутер `/api/achievements/`.
- Mobile: экран `achievements.tsx` + блоки в `profile.tsx` и `user/[username].tsx` + анлок-анимация + push.
- Прогнать 7 тестовых сценариев (§10.4) на staging.
- Деплой Phase 0 на прод. Бэкфилл существующих юзеров (выдать A1/A2 если уже есть пластинки/вишлисты).

