# Claude Code context layer (V1) — local artifacts
.claude/mcp/.venv/
.claude/mcp/index.db
.claude/mcp/index.db-journal
.claude/mcp/__pycache__/
.claude/mcp/*.pyc
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# OS / editor
.DS_Store

# Design pack zip — slim by reference, generated from Design/style-pack-achievements/
Design/style-pack-achievements.zip

# Tier 2 tracklist ingest (large, local-only)
tier2_target_ids.txt
tracklists.csv
*_releases.xml.gz

# blue-green: runtime-состояние активного цвета живёт только на проде
# (deploy.sh пишет/читает). Держим вне git, чтобы не конфликтовать со stash/pop
# при git pull. active_upstream.conf ОСТАЁТСЯ в репо — он нужен для nginx-mount.
Backend/nginx/.active_color
